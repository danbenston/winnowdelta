# Normalize all text to LF in the repo (this tool exists partly to kill
# LF<->CRLF diagnostic spam; keep our own house in order).
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.cmd text eol=crlf
*.ps1 text eol=crlf
